var x; 
console.log( x );
console.log( undefined );

var y = null;
console.log( y );

var obj = {
    x: 1
};

obj = null;

// null basically tells the program that the var or obj, even if previously defined, is now null and/or "garbage"

